server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Serve the Histocell diagnostic page as the site root
    index /histocell/index.html;

    location / {
        try_files $uri $uri/ /histocell/index.html;
    }
}
